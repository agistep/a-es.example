package io.agistep.event.sed;

import io.agistep.event.Event;
import io.agistep.event.FooCreated;
import io.agistep.event.TestEvents;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

class JsonToEventTest {


    @Test
    void name() {
//        Event event = TestEvents.anEvent(1, 1, new FooCreated());
//
//        String serialized = ConvertUtil.convert(event);
//        Event deserialized = ConvertUtil.convert(serialized);
//
//        assertThat(event).isEqualTo(deserialized);
    }
}